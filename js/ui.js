export class UI {
  //Kurucu method
  constructor() {
    this.form = document.querySelector("form");
  }

  // Sarki verilerini render eden bir fonk. yaz
  renderCards(songs) {
    songs.forEach((song) => {
      //Bir tane div olustur
      const card = document.createElemenet("div");
    });
  }
}
